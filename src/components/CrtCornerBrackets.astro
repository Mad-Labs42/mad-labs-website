---
// CrtCornerBrackets — decorative HUD-style corner brackets on the CRT screen.
//
// Purely decorative (aria-hidden), pointer-transparent. State coupling: parent
// CrtHero's state machine sets is-major-glitching / is-restoring / is-stable on
// [data-crt-hero]; the :global() rules below react to those ancestor classes.
---

<div class="crt-corner crt-corner--tl" aria-hidden="true"></div>
<div class="crt-corner crt-corner--tr" aria-hidden="true"></div>
<div class="crt-corner crt-corner--bl" aria-hidden="true"></div>
<div class="crt-corner crt-corner--br" aria-hidden="true"></div>

<style>
  .crt-corner {
    position: absolute;
    z-index: 30;
    width: 19px;
    height: 19px;
    pointer-events: none;
    border-color: #9333ea;
    border-style: solid;
    opacity: 0.6;
    transition:
      opacity 0.38s ease,
      transform 0.38s ease;
  }

  :global(.is-major-glitching) .crt-corner,
  :global(.is-restoring) .crt-corner {
    opacity: 0;
    transform: scale(0.92);
  }

  /* Corner brackets remain subtly visible in stable state */
  :global(.is-stable) .crt-corner {
    opacity: 0.2;
    transform: scale(1);
  }

  .crt-corner--tl {
    top: 16px;
    left: 16px;
    border-width: 2px 0 0 2px;
  }

  .crt-corner--tr {
    top: 16px;
    right: 16px;
    border-width: 2px 2px 0 0;
  }

  .crt-corner--bl {
    bottom: 8px;
    left: 8px;
    border-width: 0 0 2px 2px;
  }

  .crt-corner--br {
    right: 8px;
    bottom: 8px;
    border-width: 0 2px 2px 0;
  }

  @media (max-width: 640px) {
    .crt-corner {
      width: 14px;
      height: 14px;
    }

    .crt-corner--tl {
      top: 8px;
      left: 8px;
    }

    .crt-corner--tr {
      top: 8px;
      right: 8px;
    }

    .crt-corner--bl {
      bottom: 4px;
      left: 4px;
    }

    .crt-corner--br {
      right: 4px;
      bottom: 4px;
    }
  }
</style>
